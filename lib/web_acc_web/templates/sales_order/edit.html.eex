<h1>Edit Sales order</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.sales_order_path(@conn, :update, @sales_order)) %>

<span><%= link "Back", to: Routes.sales_order_path(@conn, :index) %></span>
