<h1>New Purchase order</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.purchase_order_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.purchase_order_path(@conn, :index) %></span>
